{
  "name": "centralcats-id",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Situs statis Central Cat's — vitest hanya untuk menguji invarian repo, BUKAN build tool. Situs tetap di-deploy apa adanya dari root.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}

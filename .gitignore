# OS junk
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Temp / kerja lokal (mis. screenshot verifikasi)
.perf-tmp/
*.tmp
*.log

# Pratinjau 260px yang dihasilkan tools/og-card.py untuk uji keterbacaan.
# Alat kerja, bukan aset situs — kartunya sendiri (share-card.jpg) tetap masuk.
*-pratinjau-260.png

# Bytecode Python dari tools/ (repo di-deploy apa adanya; ini tak boleh ikut)
__pycache__/
*.pyc

# Dependensi tes (vitest). Situs TIDAK butuh ini untuk jalan —
# GitHub Pages tetap men-deploy file statis dari root apa adanya.
node_modules/

# Catatan kerja lokal AI-agent (jangan masuk repo — repo publik)
AGENTS.md
CLAUDE.md

# Rahasia — repo ini PUBLIK. Satu .env yang ter-commit langsung terbaca
# seluruh internet, dan riwayat git tak bisa dibersihkan diam-diam.
# Per 19 Agu 2026 belum pernah ada .env di sini; ini PAGAR, bukan tambalan.
.env
.env.*
!.env.example
